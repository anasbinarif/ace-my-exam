"use client";

import { Box, styled, Typography } from "@mui/material";

export const AdminContentWrapper = styled(Box)({
  width: "100%",
  height: "100%",
  padding: "2rem",
});

export const AdminContentBox = styled(Box)({
  position: "relative",
  width: "100%",
  padding: "1.2rem",
  backgroundColor: "white",
  borderRadius: "1rem",
  boxShadow: "0px 4px 39.5px 0px rgba(0, 0, 0, 0.10)",
});
